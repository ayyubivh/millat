enum AddressNavType {
  checkout,
  profile,
  editAddress,
}

enum FilterType {
  category,
  brand,
}

enum Qurantype {
  para,
  sura,
  verse,
}

enum ExpandTypeonBookmark {
  first,
  second,
}

enum BookMarkCollectionType {
  add,
  edit,
}

enum QuranFavbookMarkType {
  view,
  add,
}
